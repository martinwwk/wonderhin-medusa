import {Checkbox} from "@/components/shared/form/checkbox";import cn from "classnames";import React from "react";export interface ColorOption {    id: string    label: string    count: number    hex: string}interface ColorsFilterProps {    colors: ColorOption[]    selectedColors: Record<string, boolean>    onColorChange: (id: string, checked: boolean) => void}export function ColorsFilter({ colors, selectedColors, onColorChange }: ColorsFilterProps) {    return (        <div className=" flex flex-wrap gap-3 items-center">            {colors.map((color) => (                <div key={color.id} className="flex items-center">                    <input                        type="checkbox"                        id={`color-${color.id}`}                        checked={selectedColors[color.id] || false}                        onChange={(e) => onColorChange(color.id, e.target.checked)}                        className="peer sr-only"                    />                    <label                        htmlFor={`color-${color.id}`}                        className={cn(                            `peer-checked:border-brand-dark`,                            "p-1 border bg-white rounded-full border-gray-300  ")}                    >                        <div className={cn("relative cursor-pointer w-6 h-6 rounded-full border border-brand-dark/10")}                            style={{ backgroundColor: color.hex }}                        >                            <span className="sr-only">{color.label}</span>                        </div>                    </label>                </div>            ))}        </div>    )}