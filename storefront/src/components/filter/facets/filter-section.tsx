import type { ReactNode } from "react"import { Minus, Plus } from "lucide-react"interface FilterSectionProps {    title: string    isOpen: boolean    onToggle: () => void    children: ReactNode}export function FilterSection({ title, isOpen, onToggle, children }: FilterSectionProps) {    return (        <div className="relative flex flex-col pb-6 border-b last:border-0 border-gray-200 space-y-7">            <div className="block-title  flex items-center justify-between cursor-pointer" onClick={onToggle}>                <h3 className="font-medium  text-base   text-brand-dark">{title}</h3>                <button                    className=" flex items-center justify-center"                    aria-label={isOpen ? `Collapse ${title.toLowerCase()}` : `Expand ${title.toLowerCase()}`}                >                    {isOpen ? <Minus size={18} absoluteStrokeWidth/>: <Plus size={18} absoluteStrokeWidth/>}                </button>            </div>                        {isOpen && <div className="space-y-4">{children}</div>}        </div>    )}